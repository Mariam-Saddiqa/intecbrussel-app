import React from 'react';
import ReactDOM from 'react-dom';
import MainApplication from './new'

ReactDOM.render(
  <React.StrictMode>
    <MainApplication />
  </React.StrictMode>,
  document.getElementById('root')
);


